name: Release

# Mirrors the fleet release workflow (gutenberg_kg, doc_kg) with one deliberate
# difference: there is no build step, and so no `publish` job either.
#
# Those repos ship an importable package (`packages = [{ include = ... }]`),
# build a wheel + sdist, and — as of 2026-08 — publish it to PyPI via trusted
# publishing (OIDC, `pypa/gh-action-pypi-publish`, a `pypi` environment; see
# doc_kg/.github/workflows/release.yml for the canonical shape). corpus_pepys
# sets `package-mode = false` and has no src/ tree — it ships a corpus and a
# Docker image, not a distributable — so `poetry build` fails outright with
# "Building a package is not possible in non-package mode", and there is
# nothing for a publish job to upload. Do not add either step back expecting
# them to work as-is.
#
# The Docker image is published separately (make build-image && docker push);
# this workflow only cuts the GitHub Release from the tagged commit.
#
# PLANNED: corpus_pepys is slated to become a package — docker/*.py moving to
# src/corpus_pepys/serve/, the Dockerfile switching from loose COPYs to
# `pip install .`. When that lands, restore the Poetry install + `poetry build`
# steps and the `publish` job, matching doc_kg exactly (including registering
# corpus_pepys as a trusted publisher on PyPI first).

on:
  push:
    tags: ['v*']

permissions:
  contents: write  # needed to create GitHub Releases

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      # The tag and the version declared at the tagged commit must agree, or the
      # Release is named for a version the code does not claim. With no build
      # artifacts to catch a mismatch, this is the only guard the workflow has.
      - name: Verify tag matches pyproject version
        run: |
          TAG="${GITHUB_REF_NAME#v}"
          DECLARED=$(grep -m1 '^version = ' pyproject.toml | cut -d'"' -f2)
          if [ "$TAG" != "$DECLARED" ]; then
            echo "::error::tag v$TAG does not match pyproject.toml version $DECLARED"
            exit 1
          fi
          echo "tag v$TAG matches pyproject.toml"

      - name: Create GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="${{ github.ref_name }}"
          if gh release view "$TAG" &>/dev/null; then
            echo "Release $TAG already exists — updating notes"
            gh release edit "$TAG" --notes-file release-notes.md
          else
            gh release create "$TAG" \
              --title "corpus_pepys $TAG" \
              --notes-file release-notes.md
          fi
