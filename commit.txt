feat(docker): image gen, timing display, resolution picker, IMAGE_STEPS (v0.3.0)

- image_gen.py, image_server.py: in-process Flux2Klein server; vlm_rewrite()
  for VLM-assisted prompt rewriting before FLUX; filepath response format
- chat.py: 🎨 Render response button with aspect-ratio picker; 🖼️ Resolution
  sidebar selectbox (Preview 768×512 / Standard 1152×768 / Full 1536×1024);
  💾 Save result download; 🗑️ Clear button; full diary passage text in hits
- handler.py: op=imagine proxy, _attach_content full-text fetch, op=models
  live model listing, search_ms/synthesis_ms timing in every response
- Unified IMAGE_STEPS env var (was GUTENKG_IMAGE_STEPS in image_gen/server);
  chat.py forwards num_inference_steps per-request; docker-compose wires it
- Timing captions: search/synthesis ms in result footer; VLM and image gen
  ms shown after each render
- CI: GitHub Actions workflow (ruff lint/format, ty type-check, hadolint +
  docker-compose validate); pre-commit mypy → ty

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>
